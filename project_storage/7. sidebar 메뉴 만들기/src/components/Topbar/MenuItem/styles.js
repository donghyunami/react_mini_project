import styled from '@emotion/styled';

export const ItemStyle = styled.li`
  font-size: 15px;
  padding: 0.2em 1.2em;
  position: relative;
  top: 0;
  left: 0;

  button {
    font-size: inherit;
    border: none;
    background: transparent;
    cursor: pointer;
  }
`;
